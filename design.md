# 部品在庫管理アプリ - UI/UX設計ドキュメント

## 設計原則

- **現場最適化**: 片手操作可能、最小ステップ（3タップ目標）での入力
- **スマホ縦向き（9:16）**: すべてのスクリーンを縦向きで設計
- **iOS HIG準拠**: Apple Human Interface Guidelinesに従った直感的なデザイン
- **入力速度優先**: よく使う部品・ナンバー履歴を優先表示

---

## スクリーン一覧

### 1. **TOP画面（ホーム）**
メインメニュー画面。5つのアクションボタンを使用頻度順に配置。

**主要要素:**
- 大きなボタン（タップ面積 60×60pt以上）
- 使用頻度順の配置：
  1. 部品を使った（出庫入力）
  2. 部品を仕入れた（入庫入力）
  3. 在庫を確認
  4. 履歴を見る（ナンバー検索）
  5. 月末処理

**機能:**
- ボタンタップで各画面へ遷移
- ハプティクスフィードバック付き

---

### 2. **出庫入力画面**
部品使用時の入力フロー。

**入力項目:**
- 日付：自動入力（本日日付）
- 伝票番号：テキスト入力
- 顧客名：ナンバー入力で自動表示（検索機能付き）
- 車両ナンバー（下4桁）：テキスト入力
- 部品名：選択UI（よく使う部品→車両履歴→検索）
- 数量：＋/−ボタン + テキスト入力

**UI特性:**
- ナンバー入力後、過去の顧客名が自動表示
- 部品選択UI：タップ優先、最小操作設計
- 数量入力：基本は「1」初期表示、小数対応フラグで0.1単位入力可能

---

### 3. **入庫入力画面**
部品仕入時の入力フロー。

**入力項目:**
- 日付：自動入力（本日日付）
- 伝票番号：テキスト入力
- 仕入先：テキスト入力
- 部品名：選択UI（よく使う部品→検索）
- 数量：＋/−ボタン + テキスト入力

**UI特性:**
- 出庫入力と同様の部品選択UI
- 数量入力は出庫と同じ仕様

---

### 4. **部品選択UI（モーダル）**
出庫・入庫入力時に表示される部品選択画面。

**3段階選択:**
1. **よく使う部品（上位5件）**: タップで即選択
2. **車両履歴（ナンバー入力後表示、最大5件）**: 過去に使用した部品を表示
3. **検索（インクリメンタル）**: 部品名で検索

**UI特性:**
- セクション分けで視認性向上
- タップ操作優先、スクロール最小化
- 検索時はリアルタイムフィルタリング

---

### 5. **在庫一覧画面**
全部品の現在在庫状態を表示。

**主要要素:**
- 部品ごとの現在在庫数
- 在庫状態表示：
  - 適正：緑色
  - 不足（最低在庫以下）：黄色 + 「発注推奨」ラベル
  - マイナス在庫：赤色 + 警告アイコン
- 検索機能（部品名でフィルタリング）

**リスト構成:**
- 部品名、品番、単価、現在在庫数、状態を表示
- スクロール可能なリスト形式

---

### 6. **ナンバー検索・履歴画面**
車両ナンバー（下4桁）で過去の使用履歴を検索。

**入力:**
- ナンバー入力フィールド（下4桁）

**表示内容:**
- 使用履歴一覧（日付順・降順）
- 各行に：日付、伝票番号、顧客名、部品名、数量を表示

**UI特性:**
- ナンバー入力後、自動的に履歴を検索・表示
- リスト形式で日付順にソート

---

### 7. **月末処理画面**
月末在庫確定・翌月繰越・CSV出力。

**主要要素:**
- 月末在庫確定ボタン
- 翌月へ繰越ボタン
- CSV出力ボタン（複数形式）

**機能:**
- 月末在庫を確定
- 翌月へ自動繰越
- 使用履歴・仕入履歴をCSV出力

**CSV出力形式:**
- 使用履歴：日付,伝票番号,顧客名,ナンバー,部品名,数量
- 仕入履歴：日付,伝票番号,仕入先,部品名,数量

---

## 主要ユーザーフロー

### フロー1: 部品使用（出庫）
1. TOP画面 → 「部品を使った」ボタンタップ
2. 出庫入力画面表示
3. ナンバー入力 → 顧客名自動表示
4. 部品選択UI表示 → よく使う部品から選択（またはナンバー履歴から選択）
5. 数量入力（＋/−ボタン）
6. 「保存」ボタンで確定
7. 在庫自動減算、履歴に記録

### フロー2: 部品仕入（入庫）
1. TOP画面 → 「部品を仕入れた」ボタンタップ
2. 入庫入力画面表示
3. 仕入先入力
4. 部品選択UI表示 → よく使う部品から選択
5. 数量入力（＋/−ボタン）
6. 「保存」ボタンで確定
7. 在庫自動加算、履歴に記録

### フロー3: 在庫確認
1. TOP画面 → 「在庫を確認」ボタンタップ
2. 在庫一覧画面表示
3. 部品ごとの在庫状態を確認
4. 発注推奨・警告表示を確認

### フロー4: ナンバー検索
1. TOP画面 → 「履歴を見る」ボタンタップ
2. ナンバー検索画面表示
3. ナンバー（下4桁）入力
4. 過去の使用履歴を日付順に表示

### フロー5: 月末処理
1. TOP画面 → 「月末処理」ボタンタップ
2. 月末処理画面表示
3. 「月末確定」ボタン → 在庫確定
4. 「翌月繰越」ボタン → 翌月へ自動繰越
5. 「CSV出力」ボタン → 使用履歴・仕入履歴をダウンロード

---

## カラーパレット

| 用途 | 色 | 用途詳細 |
|------|-----|---------|
| **Primary** | #0066CC | ボタン、アクティブ状態 |
| **Background** | #FFFFFF | 画面背景 |
| **Surface** | #F5F5F5 | カード、入力フィールド背景 |
| **Foreground** | #1A1A1A | テキスト（主） |
| **Muted** | #666666 | テキスト（副）、ラベル |
| **Border** | #E0E0E0 | 枠線、区切り線 |
| **Success** | #22C55E | 適正在庫、成功状態 |
| **Warning** | #F59E0B | 発注推奨、警告 |
| **Error** | #EF4444 | マイナス在庫、エラー |

---

## レイアウト仕様

### TOP画面
```
┌─────────────────────┐
│  部品在庫管理アプリ   │
├─────────────────────┤
│                     │
│  ┌───────────────┐  │
│  │ 部品を使った   │  │
│  │  （出庫入力）  │  │
│  └───────────────┘  │
│                     │
│  ┌───────────────┐  │
│  │ 部品を仕入れた │  │
│  │  （入庫入力）  │  │
│  └───────────────┘  │
│                     │
│  ┌───────────────┐  │
│  │ 在庫を確認    │  │
│  └───────────────┘  │
│                     │
│  ┌───────────────┐  │
│  │ 履歴を見る    │  │
│  │ （ナンバー検索）│  │
│  └───────────────┘  │
│                     │
│  ┌───────────────┐  │
│  │ 月末処理      │  │
│  └───────────────┘  │
│                     │
└─────────────────────┘
```

### 出庫入力画面
```
┌─────────────────────┐
│ 部品を使った        │
├─────────────────────┤
│ 日付: 2026-04-13   │
│ 伝票番号: [________]│
│ ナンバー: [____]    │
│ 顧客名: ○○自動車   │
│                     │
│ 部品名: [選択]      │
│ ├ よく使う部品      │
│ │ ├ エンジンオイル  │
│ │ ├ ブレーキパッド  │
│ │ └ ...            │
│ └ 検索: [_______]   │
│                     │
│ 数量:               │
│ [−] 1 [+]          │
│                     │
│ ┌─────────────────┐ │
│ │    保存         │ │
│ └─────────────────┘ │
└─────────────────────┘
```

### 在庫一覧画面
```
┌─────────────────────┐
│ 在庫一覧            │
├─────────────────────┤
│ 検索: [_________]   │
├─────────────────────┤
│ エンジンオイル      │
│ 品番: EO-001        │
│ 単価: ¥2,500        │
│ 在庫: 15個 ✓ 適正   │
├─────────────────────┤
│ ブレーキパッド      │
│ 品番: BP-002        │
│ 単価: ¥3,800        │
│ 在庫: 2個 ⚠ 発注推奨│
├─────────────────────┤
│ ラジエーター液      │
│ 品番: RL-003        │
│ 単価: ¥1,200        │
│ 在庫: -1個 ✗ 警告   │
└─────────────────────┘
```

---

## 実装優先順位

1. **Phase 1**: TOP画面、出庫入力、入庫入力（基本フロー）
2. **Phase 2**: 部品マスタ管理、部品選択UI
3. **Phase 3**: 在庫一覧、ナンバー検索
4. **Phase 4**: 月末処理、CSV出力
5. **Phase 5**: UI最終調整、ハプティクス、アニメーション

---

## 技術仕様

- **フレームワーク**: Expo + React Native
- **状態管理**: React Context + AsyncStorage（ローカル永続化）
- **スタイリング**: NativeWind (Tailwind CSS)
- **データベース**: SQLite（ローカル）またはAsyncStorage
- **ナビゲーション**: Expo Router

---
