# 部品在庫管理アプリ - 取扱説明書

## 概要

このアプリは、部品の入庫・出庫・在庫管理を効率的に行うためのモバイルアプリケーションです。タブレットやスマートフォンで部品の流れを一元管理できます。

---

## 主な機能

### 1. **部品を使った（出庫入力）**
部品を使用・出庫した時に記録します。

**操作方法：**
1. ホーム画面から「部品を使った」をタップ
2. 以下の情報を入力：
   - **伝票番号**：出庫の識別番号
   - **顧客名**：部品を使用した顧客名
   - **車両番号**：対象の車両番号
   - **仕入先**：部品の仕入先（ドロップダウンから選択またはテキスト入力）
   - **部品名**：使用した部品（「部品を選択」ボタンから選択）
   - **数量**：使用した数量（小数点入力可能）

**複数部品の入力：**
- 「+ 部品追加」ボタンで複数の部品を一度に入力できます
- 追加した部品は下部に一覧表示されます
- 各部品の「削除」ボタンで削除可能
- 「保存」ボタンで全部品をまとめて登録

**よく使う部品：**
- 画面上部に「よく使う部品」セクションがあります
- よく使う部品をタップすると、部品が自動選択されます
- 「✏️ 編集」ボタンでよく使う部品をカスタマイズ

---

### 2. **部品を仕入れた（入庫入力）**
新しい部品を仕入れ・入庫した時に記録します。

**操作方法：**
1. ホーム画面から「部品を仕入れた」をタップ
2. 以下の情報を入力：
   - **日付**：入庫日（カレンダーから選択）
   - **仕入先**：部品の仕入先（ドロップダウンから選択またはテキスト入力）
   - **部品名**：仕入れた部品（「部品を選択」ボタンから選択）
   - **数量**：仕入れた数量（小数点入力可能）
   - **単価**：部品の単価（小数点入力可能）
   - **小数使用フラグ**：小数単位での入庫に対応

**複数部品の入力：**
- 「+ 部品追加」ボタンで複数の部品を一度に入力できます
- 追加した部品は下部に一覧表示されます
- 各部品の「削除」ボタンで削除可能
- 「保存」ボタンで全部品をまとめて登録

**よく使う部品：**
- 画面上部に「よく使う部品」セクションがあります
- よく使う部品をタップすると、部品が自動選択されます
- 「✏️ 編集」ボタンでよく使う部品をカスタマイズ

---

### 3. **在庫を確認**
現在の在庫状況を確認します。

**操作方法：**
1. ホーム画面から「在庫を確認」をタップ
2. 部品一覧が表示されます
3. 各部品の現在の在庫数を確認できます
4. 部品をタップすると詳細情報が表示されます

**部品の追加：**
- 「+ 新規部品追加」ボタンで新しい部品をマスタに登録
- 以下の情報を入力：
  - **品番**：部品の品番（一意の識別子）
  - **部品名**：部品の名称
  - **仕入先**：部品の仕入先（ドロップダウンから選択またはテキスト入力）
  - **小数使用フラグ**：小数単位での取扱いに対応

---

### 4. **履歴を見る**
入庫・出庫の履歴を確認します。

**操作方法：**
1. ホーム画面から「履歴を見る」をタップ
2. 入庫・出庫の履歴が一覧表示されます
3. 各履歴の詳細情報を確認できます
4. 検索機能で特定の部品や日付の履歴を検索可能

---

### 5. **データ処理**
データのエクスポート・インポート・バックアップを行います。

**操作方法：**
1. ホーム画面から「データ処理」をタップ

**テンプレート送信：**
- 「テンプレート送信」ボタンで、部品マスタのテンプレートをメール送信
- テンプレートに新しい部品情報を記入して返送

**インポート：**
- 「インポート」ボタンで、ファイルから部品データを読み込み
- CSV形式またはJSON形式に対応

**エクスポート：**
- 「エクスポート」ボタンで、現在のデータをファイルに出力
- CSV形式またはJSON形式で保存可能

**バックアップ：**
- 「バックアップ」ボタンで、全データをバックアップ
- クラウドストレージに自動保存

---

## 仕入先入力のコツ

### ダブルタップで選択
仕入先ドロップダウンが表示されたら、以下の手順で選択します：

1. **仕入先フィールドをタップ** — ドロップダウンが表示されます
2. **「📱 ダブルタップで選択」メッセージを確認**
3. **候補をダブルタップ** — 仕入先が入力フィールドに反映されます

### 新規仕入先の入力
- ドロップダウンに表示されていない仕入先は、フィールドに直接テキスト入力できます
- 入力後、「保存」ボタンで新規仕入先として登録されます

---

## よく使う部品の管理

### よく使う部品の編集
1. 出庫・入庫画面の「✏️ 編集」ボタンをタップ
2. モーダルが開き、部品一覧が表示されます
3. チェックボックスで部品を選択・解除
4. 「保存」ボタンで変更を確定

### よく使う部品の表示
- よく使う部品は**品番**で表示されます
- タップすると、その部品が自動選択されます

---

## トラブルシューティング

### ドロップダウンが表示されない
- 仕入先フィールドをタップして、テキストを入力してください
- 入力中にドロップダウンが表示されます

### 複数部品入力ができない
- 「+ 部品追加」ボタンが表示されているか確認してください
- 部品を追加した後、「保存」ボタンで登録してください

### データが保存されない
- インターネット接続を確認してください
- アプリを再起動してみてください
- 「データ処理」でバックアップを実行してください

---

## 更新履歴

### v1.0.7
- 複数部品入力機能を追加
- よく使う部品を品番表示に変更
- 仕入先ドロップダウンにダブルタップ説明を追加
- タブレット環境での表示を最適化

### v1.0.6
- 仕入先ドロップダウン機能を追加
- よく使う部品の編集機能を追加

### v1.0.5
- 初版リリース

---

## サポート

ご不明な点やご要望がございましたら、お気軽にお問い合わせください。
