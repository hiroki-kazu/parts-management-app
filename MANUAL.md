# 部品在庫管理 取扱説明書

## 1. 概要

本アプリは、自動車整備工場における部品の入庫・出庫・在庫確認・履歴管理を行うためのモバイルアプリです。データは端末内に保存され、部品マスタや履歴はCSVで一括登録できます。

| 画面 | 主な用途 |
|---|---|
| Home | 出庫、入庫、在庫、履歴、データ処理を開始します。 |
| Parts | 部品マスタを確認・編集します。 |
| Help | 操作方法の確認、チュートリアル表示、PDF取扱説明書の出力を行います。 |

## 2. 部品を使った（出庫入力）

部品を使用した時に、出庫履歴を登録して在庫を減らします。入力する項目は、**日付・伝票番号・車両ナンバー下4桁・顧客名・部品・数量**です。

1. Home画面の「部品を使った」を開きます。
2. 日付、伝票番号、車両ナンバー下4桁、顧客名を入力します。
3. 部品を選択し、使用数量を入力します。
4. 複数部品を登録する場合は「＋ 部品追加」を押し、次の部品を選択します。
5. 追加部品一覧を確認し、「保存」でまとめて登録します。

「よく使う部品」は品番で表示されます。表示内容は「編集」から変更できます。

## 3. 部品を仕入れた（入庫入力）

部品を仕入れた時に、入庫履歴を登録して在庫を増やします。入力する項目は、**日付・伝票番号・仕入先・部品・数量**です。

1. Home画面の「部品を仕入れた」を開きます。
2. 日付、伝票番号、仕入先を入力します。
3. 仕入先は直接入力できます。候補から選ぶ場合は対象をダブルタップします。
4. 部品を選択し、数量を入力します。
5. 複数部品を登録する場合は「＋ 部品追加」を押します。
6. 追加部品一覧を確認し、「保存」でまとめて登録します。

## 4. 在庫を確認

在庫一覧は確認専用です。部品ごとに、品番・部品名・仕入先・単価・現在庫・最低在庫・在庫状態を表示します。在庫数は出庫入力と入庫入力によって更新されます。

| 表示 | 意味 |
|---|---|
| 適正 | 現在庫が最低在庫を上回っています。 |
| 発注推奨 | 現在庫が最低在庫以下です。 |
| 警告 | 在庫がマイナスです。 |

## 5. 部品マスタ

画面下部の「Parts」から部品マスタを確認・編集します。部品名、品番、仕入先、単価、最低在庫、小数数量への対応を管理できます。

品番は履歴CSVインポート時の照合に使用します。重複しない値を設定してください。

## 6. 履歴を見る

出庫履歴と入庫履歴を切り替え、期間を指定して確認できます。出庫履歴には、日付・伝票番号・車両ナンバー・顧客名・部品名・数量が表示されます。

1. Home画面の「履歴を見る」を開きます。
2. 「出庫履歴」または「入庫履歴」を選択します。
3. 必要に応じて車両ナンバー下4桁と期間を指定します。
4. 「検索」を押して履歴一覧を表示します。

## 7. データ処理とCSV

データ処理画面では、部品マスタ、出庫履歴、入庫履歴のCSVテンプレート送信とインポート、期間指定ZIP出力を行います。CSVはUTF-8・Shift-JIS、カンマ・タブ区切りに対応しています。

| CSV | 列構成 |
|---|---|
| 出庫履歴 | 日付、伝票番号、車両ナンバー、顧客名、品番、部品名、数量 |
| 入庫履歴 | 日付、伝票番号、仕入先、部品名、品番、数量 |

CSVの列名は変更せず、2行目以降にデータを入力してください。履歴インポートでは品番を優先して部品マスタと照合します。完了画面の「履歴を確認」を押すと、保存された履歴を確認できます。

期間指定エクスポートでは、出庫履歴・入庫履歴・在庫サマリーをZIPにまとめてメール送信または保存できます。

## 8. PDF取扱説明書の保存

Help画面上部の「取扱説明書をPDFで保存」を押すと、この取扱説明書をPDFとして生成します。Androidの共有画面から、端末への保存、メール送信、クラウドドライブへの保存を選択できます。

PDFのファイル名は `部品在庫管理_取扱説明書.pdf` です。

## 9. チュートリアル

初回にHelp画面を開くと、主要機能を説明するチュートリアルが表示されます。再確認する場合は、Help画面上部の「チュートリアルをもう一度見る」を押してください。

## 10. トラブルシューティング

| 問題 | 確認内容 |
|---|---|
| CSVを取り込めない | 列名を変更していないか、品番が部品マスタに登録されているか確認します。 |
| CSVが空と表示される | ヘッダーの次の行からデータを入力します。 |
| 履歴が表示されない | 履歴の種類、期間、車両ナンバー条件を確認して再検索します。 |
| 仕入先を候補から選べない | 候補をダブルタップします。 |
| 在庫数が合わない | 入庫・出庫履歴と各数量を確認します。 |

## 11. データ保管上の注意

データは端末内に保存されます。端末の故障・紛失・アプリ削除に備え、データ処理画面から定期的にZIPを出力し、端末外へ保管してください。
