# 部品在庫管理アプリ - 実装機能プロンプト一覧

## 実装済み機能

### 1. 履歴検索機能の改善
**プロンプト**: 履歴画面の検索機能を改善し、ナンバー検索と期間検索を独立させ、どちらか一方だけでも検索できるようにする

**実装内容**:
- ナンバーと期間を独立させた検索ロジック
- ナンバーだけで検索可能
- 期間だけで検索可能
- 両方を組み合わせて検索も可能
- ナンバー検索時は顧客情報を表示
- 期間検索時は検索条件を表示

---

### 2. 履歴削除機能
**プロンプト**: 検索した履歴を削除する機能を履歴画面に追加する

**実装内容**:
- storage.tsに削除関数を追加
  - `deleteOutboundRecord(recordId)`: 単一の履歴を削除
  - `deleteOutboundRecords(recordIds)`: 複数の履歴を削除
- search.tsxに削除ボタンを追加（赤色）
- 削除ボタンをタップすると確認ダイアログが表示
- 確認後に履歴が削除され、検索結果が更新される

---

### 3. 月末処理画面の日付表示改善
**プロンプト**: 月末処理の期間指定の開始日と終了日の日付入力部分の文字の色を黒にして下さい

**実装内容**:
- monthend.tsxの日付入力部分の文字色を黒に変更
- コントラストが向上し、日付がより見やすくなった

---

### 4. CSV出力ファイル保存問題の修正
**プロンプト**: 月末処理のcsv出力で、csvファイルがダウンロードフォルダに保存されましたと、表示されるが確認するとファイルがない問題を修正

**実装内容**:
- ファイル保存処理を改善
- キャッシュディレクトリにファイルを保存
- プラットフォーム別の処理を追加
- iOS/Android対応
  - メディアライブラリ（ダウンロードフォルダ）にファイルを保存
  - パーミッション要求を追加
  - フォールバック処理でShare APIを使用
- Web対応：ブラウザのダウンロード機能を使用

---

### 5. 履歴画面の機能拡張
**プロンプト**: 履歴で入庫履歴も確認出来るように出来ますか？入力部分がテンキーで隠れないように出来ますか？

**実装内容**:
- 入庫履歴表示機能
  - 履歴画面に「出庫履歴」「入庫履歴」のタブを追加
  - タブ切り替えで出庫履歴と入庫履歴を切り替え可能
  - 入庫履歴では仕入先を表示
- テンキー対策
  - KeyboardAvoidingViewで画面全体をラップ
  - ScrollViewにkeyboardShouldPersistTaps="handled"を設定
  - keyboardDismissMode="interactive"で対話的なキーボード閉じを実装
  - contentContainerStyleでpaddingBottomを追加してスペース確保
- 検索機能の改善
  - 出庫履歴タブではナンバー検索が可能
  - 入庫履歴タブではナンバー検索は非表示
  - 期間検索は両方のタブで使用可能

---

### 6. 在庫表示の浮動小数点誤差修正
**プロンプト**: 在庫数がおかしくなっています。検証して下さい

**実装内容**:
- JavaScriptの浮動小数点演算による誤差を修正
- 在庫一覧画面（inventory.tsx）
  - 小数点対応部品：小数第1位まで表示（Math.round(stock * 10) / 10）
  - 整数部品：整数のみ表示（Math.round(stock)）
- 部品マスタ管理画面（parts.tsx）
  - 同じフォーマット処理を適用
  - 浮動小数点誤差を削除
- 「7.1999999999999」のような誤差表示が「7.2」と正確に表示されるようになった

---

### 7. 出庫入力の伝票番号自動検索機能
**プロンプト**: 出庫入力時、伝票番号が同じ場合、履歴からナンバーと顧客名を自動的に表示する事は出来ますか？

**実装内容**:
- storage.tsに検索関数を追加
  - `getOutboundRecordByVoucherNumber()`: 伝票番号で最新の出庫履歴を検索
- 出庫入力画面に自動入力ロジックを実装
  - 伝票番号入力時に自動的に履歴から検索
  - 同じ伝票番号の記録が見つかった場合、ナンバーと顧客名を自動入力
  - useEffectで伝票番号の変更を監視

---

### 8. 部品選択後の自動フォーカス移動
**プロンプト**: 同じ伝票番号で複数の部品を連続入力する際、部品を選択した後に自動的に次の入力フォームに移動するようにしてください

**実装内容**:
- 出庫入力画面に部品選択後の自動スクロール機能を実装
- 部品選択後に数量入力フィールドに自動的にフォーカスが移動
- ScrollViewを使用してスムーズなスクロール

---

### 9. Androidのダウンロード問題修正
**プロンプト**: Androidですが、確認できません（CSVファイルがダウンロードフォルダに見つからない問題）

**実装内容**:
- Androidのダウンロード処理を改善
- `FileSystem.documentDirectory`を基準にダウンロードフォルダへのパスを構築
- ダウンロードフォルダが存在しない場合は自動作成
- CSVファイルをダウンロードフォルダに直接保存
- プラットフォーム別の処理を明確化
  - Android: ダウンロードフォルダに直接保存
  - iOS: メディアライブラリ（写真アプリ）に保存
  - Web: ブラウザのダウンロード機能を使用

---

### 10. ZIP一括ダウンロード機能
**プロンプト**: 複数ファイル一括ダウンロード: 3つのCSVファイル（出庫・入庫・在庫サマリー）をZIPで圧縮して一度にダウンロード

**実装内容**:
- react-native-zip-archiveライブラリをインストール
- 月末処理画面に「全ファイルをZIPでダウンロード」ボタンを追加
- プラットフォーム判定を追加
  - Web環境ではZIP機能を無効化（個別ダウンロードを推奨）
  - Android: ダウンロードフォルダに直接保存
  - iOS: メディアライブラリに保存
- 3つのCSVファイルを一時ディレクトリに保存してZIPで圧縮
- UI改善
  - 「全ファイルをZIPでダウンロード」ボタンをオレンジ色で強調表示
  - 個別ダウンロードボタンと一括ダウンロードボタンを明確に区別

---

## 機能別チェックリスト

- [x] 履歴検索機能の改善（ナンバー・期間独立検索）
- [x] 履歴削除機能
- [x] 月末処理画面の日付表示改善
- [x] CSV出力ファイル保存問題の修正
- [x] 履歴画面の機能拡張（入庫履歴・テンキー対策）
- [x] 在庫表示の浮動小数点誤差修正
- [x] 出庫入力の伝票番号自動検索機能
- [x] 部品選択後の自動フォーカス移動
- [x] Androidのダウンロード問題修正
- [x] ZIP一括ダウンロード機能

---

## 次のステップ提案

### 短期（すぐに実装可能）
1. **ダウンロード進捗表示**: ZIP作成時にプログレスバーを表示
2. **検索履歴の保存**: よく検索するナンバーや期間を保存
3. **複数部品フィルタ**: 特定の部品だけを検索対象にできるフィルタ

### 中期（1-2週間）
1. **ダウンロード履歴管理**: 過去のダウンロードファイルを一覧表示
2. **メール送信機能**: 作成したZIPファイルをメールで送信
3. **在庫監視アラート**: 在庫が最低在庫以下になった場合に通知

### 長期（1ヶ月以上）
1. **在庫計算の精度向上**: 浮動小数点誤差を完全に排除する内部管理方式
2. **在庫トレンド分析**: 月別の在庫推移グラフを表示
3. **複数ユーザー対応**: ユーザー認証とロール管理機能
